import { featuresData } from "@/components/propertyPage/noSql/currentPropertyData"
import PropertyFeatures from "../ui/propertyFeatures"

const PropertyFeaturesTable = () => {
  return <PropertyFeatures features={featuresData} />
}

export default PropertyFeaturesTable
